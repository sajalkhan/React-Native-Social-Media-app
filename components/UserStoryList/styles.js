import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  userStoryWrapper: {
    marginTop: 10,
    marginBottom: 30,
    minHeight: 70,
    width: '100%',
    height: 'auto',
  },
});

export default style;
